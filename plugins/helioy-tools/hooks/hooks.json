{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd ~/Dev/LLM/DEV/helioy && just preflight >/dev/null 2>&1 && STATUS='Preflight: ok' || STATUS='Preflight: FAILED (run just preflight to debug)'; echo \"${STATUS}\"; echo 'Your primary memory is context-matters (cm) via cx_* MCP tools. Call cx_recall NOW with the dirname you are working in. This ensures you have the most relevant context for your current task. to retrieve relevant context from prior sessions. Do this BEFORE responding or exploring the codebase. After meaningful work, call cx_deposit to persist facts, decisions, and lessons learned.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "fmm generate; true"
          }
        ]
      }
    ],
    "SessionEnd": []
  }
}
---
name: nancy-session-history
description: Access session history to continue work, see what happened, or search past sessions. Use this instead of relying on memory — the raw data is always more accurate.
---

# Session History

Use `git log --format=full -10` to see what happened recently.

Remember to make your git commit messages count — they are the session history.

## Quick Commands

```bash
git log --format=full -10
```
